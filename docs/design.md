# english-learning-app 設計ドキュメント

## 1. 機能仕様

### Phase 1: コア機能（MVP） - 実装完了

| 機能 | 説明 | 状態 |
|------|------|------|
| 単語クイズ | 3種類の問題タイプ（英→日/日→英/穴埋め）で学習 | ✅ |
| スピードチャレンジ | 30秒間で何問正解できるか挑戦 | ✅ |
| 学習進捗の記録 | 正誤履歴をlocalStorageに保存 | ✅ |
| 単語データベース | TOEIC基礎単語の初期データセット | ✅ |
| 単語詳細画面 | 音声・画像・例文・類義語・コラムによる深い学習 | ✅ |
| 音声読み上げ | 出題時自動再生＋手動再生ボタン | ✅ |
| イメージ画像 | カテゴリ別プレースホルダー画像表示 | ✅ |
| 学習統計 | タブ切り替えで概要/苦手単語/履歴を表示 | ✅ |
| 苦手単語機能 | 正答率70%未満の単語を抽出・復習 | ✅ |

### Phase 2: ゲーミフィケーション基盤 - 実装完了

| 機能 | 説明 | 状態 |
|------|------|------|
| XP・レベルシステム | 正解数・コンボに応じてXPを付与、レベルアップ演出 | ✅ |
| ストリーク | 連続学習日数をカウント | ✅ |
| デイリー目標 | 日替わりの学習目標（10問正解する） | ✅ |
| 実績システム | 条件達成で実績解除、ポップアップ表示 | ✅ |

### MVP時点で「やらないこと」

- ユーザー認証（一人プレイ前提）
- リアルタイム対戦機能
- ランキング機能
- ネイティブアプリ対応
- 音声認識による発音練習
- 有料プラン・課金機能

---

## 2. 出題画面仕様

### 2.1 問題タイプ

| タイプ | 説明 | 出題比率 |
|--------|------|----------|
| Type A: 英語→日本語 | 英単語を見て日本語の意味を選ぶ | 50% |
| Type B: 日本語→英語 | 日本語を見て英単語を選ぶ | 30% |
| Type C: 穴埋め | 例文の空欄に入る単語を選ぶ | 20% |

### 2.2 音声読み上げ仕様

#### 自動再生

| 画面 | 問題タイプ | 自動再生 | 読み上げ内容 | 遅延 |
|------|----------|---------|-------------|------|
| クイズに挑戦 | 英→日 | ✅ | 英単語 | 300ms |
| クイズに挑戦 | 日→英 | ❌ | なし | - |
| クイズに挑戦 | 穴埋め | ❌ | なし（手動ボタンで再生可） | - |
| スピードチャレンジ | 英→日 | ✅ | 英単語 | 100ms |
| スピードチャレンジ | 日→英 | ❌ | なし | - |

> **Note**: 穴埋め問題では、例文全体を自動再生すると答えがわかってしまうため、自動再生を無効化。ユーザーは手動の音声ボタンで再生可能。

#### 手動再生（SpeakButtonコンポーネント）

- 全画面で音声ボタンによる手動再生を維持
- ブラウザ標準Web Speech APIを使用
- 再生中は重複再生を防止

#### 技術仕様（`src/lib/audio.ts`）

```typescript
// 音声合成のサポート確認
isSpeechSynthesisSupported(): boolean

// 単語の発音（英語、速度調整可）
speakWord(word: string, options?: { slow?: boolean; onEnd?: () => void }): void

// 例文の読み上げ（英語、速度調整可）
speakSentence(sentence: string, options?: { slow?: boolean; onEnd?: () => void }): void

// 再生中かどうか
isSpeaking(): boolean

// 再生停止
stopSpeaking(): void

// 利用可能な音声の取得
getAvailableVoices(): SpeechSynthesisVoice[]
getEnglishVoice(): SpeechSynthesisVoice | null
```

### 2.3 和訳表示仕様

#### 穴埋め問題の和訳ヒント

「和訳を表示」ボタンで**例文全体の日本語訳のみ**を表示:

```
このプロジェクトはとても重要です。
```

- 例文の日本語訳のみを表示（単語の意味は表示しない）
- 単語の意味を表示すると答えが分かってしまうため、意図的に非表示

#### 誤答時の例文表示

```
Please check my schedule.
→ 私のスケジュールを確認してください。
(正解: schedule (予定、スケジュール))
```

- 英語の例文と日本語訳を両方表示
- 正解の単語と意味も表示（学習のため）
- 文脈の中で単語を理解できるようサポート

---

### 2.4 イメージ画像表示仕様

#### 画像表示フォールバックチェーン

1. **単語固有URL** (`word.imageUrl`) - 単語専用の画像がある場合
2. **キーワードベースURL** (`word.imageKeyword`) - コンセプトマッピングから検索
3. **単語名検索** - 単語自体をコンセプトマッピングで検索
4. **カテゴリURL** - カテゴリに応じたUnsplash画像
5. **絵文字フォールバック** - 読み込みエラー時にカテゴリ絵文字＋グラデーション表示

#### コンセプト画像マッピング（CONCEPT_IMAGE_URLS）

単語の意味やコアイメージに即した画像を自動選択:

| カテゴリ | 対応キーワード |
|----------|---------------|
| ビジネス関連 | meeting, schedule, deadline, project, client, contract, proposal... |
| オフィス関連 | office, desk, document, printer, employee, supervisor, colleague... |
| 金融関連 | budget, invoice, expense, profit, revenue, investment, account... |
| 旅行関連 | travel, flight, airport, hotel, reservation, passport, luggage... |
| 買い物関連 | shopping, purchase, discount, receipt, customer, refund, warranty... |
| 技術関連 | technology, software, hardware, network, database, update, install... |
| コミュニケーション | email, phone, message, confirm, notify, announce... |
| 動作・状態 | approve, submit, review, postpone, cancel, attend, participate... |

#### カテゴリ別フォールバック（`src/lib/image.ts`）

| カテゴリ | 絵文字 | 画像テーマ | グラデーション |
|----------|--------|----------|---------------|
| business | 💼 | ビジネスシーン | blue-indigo |
| office | 🏢 | オフィス | slate-gray |
| travel | ✈️ | 旅行 | cyan-sky |
| shopping | 🛒 | 買い物 | pink-rose |
| finance | 💰 | 金融 | emerald-green |
| technology | 💻 | テクノロジー | violet-purple |
| daily | 🏠 | 日常生活 | amber-yellow |
| communication | 💬 | コミュニケーション | orange-red |

#### ユーティリティ関数

```typescript
// 画像URL取得（新API - 推奨）
getImageUrl(options: {
  wordImageUrl?: string;    // 単語固有URL
  imageKeyword?: string;    // コンセプトキーワード
  word?: string;            // 単語名（コンセプト検索用）
  category?: string;        // カテゴリ（フォールバック用）
}): string | null

// 後方互換性のための旧API
getImageUrl(wordImageUrl?: string, category?: string): string | null

// カテゴリ絵文字取得
getCategoryEmoji(category?: string): string

// カテゴリグラデーション取得
getCategoryGradient(category?: string): string

// コンセプト画像の利用可否チェック
hasConceptImage(keyword: string): boolean
```

#### WordImageコンポーネント

- **サイズバリエーション**: sm (h-24), md (h-40), lg (h-48)
- **ローディング状態**: 読み込み中はアニメーション付きプレースホルダー表示
- **エラー処理**: 画像読み込み失敗時は絵文字フォールバックに切り替え
- **グラデーション背景**: カテゴリに応じた色でフォールバック表示を美しく

### 2.4 出題画面のUI構成

```
┌─────────────────────────────────────────────┐
│  ━━━━━━━━━━ 3/10 問 ━━━━━━━━━━              │ 進捗バー
├─────────────────────────────────────────────┤
│  [✨ 5 正解]  [⚡ 3連続!]                    │ スコア・コンボ
├─────────────────────────────────────────────┤
│                                             │
│  [     カテゴリ画像表示エリア      ]         │ 画像
│                                             │
│  ─────────────────────────────────────────  │
│  「この単語の意味は?」                        │ プロンプト
│                                             │
│        schedule        🔊                   │ 問題・音声ボタン
│                                             │
├─────────────────────────────────────────────┤
│  [A. 予定        ] [B. 会議        ]        │ 選択肢
│  [C. 契約        ] [D. 提案        ]        │
└─────────────────────────────────────────────┘
```

### 2.5 リザルト画面仕様

#### 表示要素

| 項目 | 説明 | 状態 |
|------|------|------|
| スコアサマリー | 正解数/総問題数、正答率、最大コンボ | ✅ |
| XP獲得表示 | 獲得したXPとレベル進捗 | ✅ |
| 全回答単語一覧 | 正解/不正解を明示した単語リスト | ✅ |
| 単語詳細への遷移 | 各単語タップで詳細画面へ | ✅ |
| 音声再生ボタン | 各単語の発音確認 | ✅ |
| 実績解除表示 | 条件達成時にポップアップ | ✅ |
| 再挑戦ボタン | 新しいクイズを開始 | ✅ |
| ホームボタン | ホーム画面へ戻る | ✅ |

#### リザルト画面の状態管理（`src/lib/quiz-session.ts`）

- **sessionStorage永続化**: リザルト画面の状態を保存し、単語詳細画面から戻った際に復元
- **30分有効期限**: 長時間放置後は新規セッションとして扱う
- **遷移元追跡**: `?from=quiz` クエリパラメータにより詳細画面からの戻り先を特定

```typescript
// リザルト状態の型
type QuizResultState = {
  score: number;
  totalQuestions: number;
  maxCombo: number;
  answeredWords: AnsweredWord[];
  sessionResult: SessionResult | null;
  timestamp: number;
};
```

#### リザルト画面のUI構成（Viewport-Fit Layout）

リザルト画面はスクロールなしで全要素にアクセスできるよう、以下の3層構造を採用:

```
┌─────────────────────────────────────────────┐
│  【固定】スコアサマリー                       │ flex-shrink-0
│   🎉 パーフェクト! セッション完了!            │
│   8/10 正答率80%  +95XP  🔥3日  🎯達成!      │
├─────────────────────────────────────────────┤
│  【スクロール可能】単語リスト・実績           │ flex-1, overflow-y-auto
│  ┌─────────────────────────────────────┐   │
│  │ ✓ schedule    予定           🔊 →  │   │
│  │ ✗ confirm     確認する       🔊 →  │   │
│  │ ✓ available   利用可能な     🔊 →  │   │
│  │ ...（内部スクロール）              │   │
│  └─────────────────────────────────────┘   │
├─────────────────────────────────────────────┤
│  【固定】アクションボタン                     │ flex-shrink-0
│  [もう一度挑戦]  [ホームに戻る]              │
└─────────────────────────────────────────────┘
```

#### 実装パターン（Tailwind CSS）

```tsx
<div className="h-[calc(100vh-64px)] px-4 py-4 flex flex-col">
  <div className="max-w-md w-full mx-auto flex flex-col h-full">
    {/* 上部固定: スコアサマリー */}
    <div className="flex-shrink-0 bg-white rounded-3xl shadow-card p-4 mb-3">
      {/* スコア表示 */}
    </div>

    {/* 中央スクロール可能エリア */}
    <div className="flex-1 overflow-y-auto min-h-0 mb-3">
      {/* 単語リスト、実績など */}
    </div>

    {/* 下部固定: アクションボタン */}
    <div className="flex-shrink-0 space-y-2">
      <Button fullWidth>もう一度挑戦</Button>
      <Button variant="secondary" fullWidth>ホームに戻る</Button>
    </div>
  </div>
</div>
```

**ポイント:**
- `h-[calc(100vh-64px)]`: ヘッダー（64px）を除いた高さを確保
- `flex-shrink-0`: 上部・下部を固定サイズに
- `flex-1 overflow-y-auto min-h-0`: 中央部分のみスクロール可能

#### クイズ問題画面のUI構成（Viewport-Fit Layout）

クイズ問題画面はスクロールなしで全要素にアクセスできるよう、**コンテンツベースの自動サイズ**レイアウトを採用:

```
┌─────────────────────────────────────────────┐
│  【固定】進捗バー・スコア                     │ flex-shrink-0
│   ━━━━━━━━━ 3/10 問  ✨5正解  ⚡3連続!      │
├─────────────────────────────────────────────┤
│  【自動】問題カード                          │ flex-shrink-0
│   [カテゴリアイコン]                         │  (コンテンツ分のみ)
│   「この単語の意味は?」                       │
│        schedule        🔊                   │
│   [A] 予定  [B] 会議  [C] 契約  [D] 提案    │
│   ← 選択肢の直下に余白なし                   │
├─────────────────────────────────────────────┤
│  【固定】フィードバック・ボタン               │ flex-shrink-0
│   🎉 正解!  +10 XP                          │
│   [次の問題へ]                              │
└─────────────────────────────────────────────┘
```

**レイアウトの重要ポイント:**
- 問題カードは `flex-shrink-0` でコンテンツに応じた自動サイズ
- **`flex-1` は使用しない**（余白の原因となるため）
- 選択肢（ABCD）の下に余白が発生しないよう設計
- 誤答時（例文＋和訳表示）でもスクロール不要

**フィードバック表示時の特徴:**
- 誤答時は例文がある場合、例文全体を音声再生
- 「次の問題へ」ボタンは常に画面内に表示（スクロール不要）
- 誤答時の例文表示は1行制限（`line-clamp-1`）でコンパクトに

---

## 2.6 画面遷移ルール（単語詳細への導線）

### 基本原則

**「単語が表示されている = その単語の詳細画面に遷移できる」**

アプリ内で単語が表示されるすべての画面において、その単語をタップすると単語詳細画面に遷移できるようにする。

### 遷移対応状況

| 画面 | 遷移箇所 | 状態 |
|------|----------|------|
| クイズリザルト | 回答した全単語リスト | ✅ |
| 学習履歴（概要タブ） | - | - |
| 学習履歴（苦手単語タブ） | 苦手単語リスト | ✅ |
| 学習履歴（履歴タブ） | 回答履歴リスト | ✅ |
| 苦手単語一覧 | 単語リスト | ✅ |
| ブックマーク一覧 | ブックマーク済み単語リスト | ✅ |
| スピードチャレンジリザルト | 正解/不正解単語リスト | ✅ |
| 単語帳 | 単語一覧 | ✅ |
| ホーム | 今日の単語など | 🔜 |

### 遷移パターン

```
/quiz（リザルト）      → /word/[id]?from=quiz      → /quiz（リザルトに復帰）
/history（苦手単語）   → /word/[id]?from=history   → /history
/history（履歴）       → /word/[id]?from=history   → /history
/weak-words           → /word/[id]?from=weak      → /weak-words
/bookmarks            → /word/[id]?from=bookmarks → /bookmarks
/speed-challenge      → /word/[id]?from=speed     → /speed-challenge
/word-list           → /word/[id]?from=wordlist   → /word-list
```

#### `from`クエリパラメータの動作

| from値 | 戻り先 | ラベル |
|--------|--------|--------|
| quiz | /quiz（リザルト復元） | リザルトに戻る |
| speed | /speed-challenge | リザルトに戻る |
| history | /history | 学習履歴に戻る |
| weak | /weak-words | 苦手単語に戻る |
| bookmarks | /bookmarks | ブックマークに戻る |
| wordlist | /word-list | 単語帳に戻る |
| (なし) | router.back() | 戻る |

---

## 3. 単語詳細画面仕様

### 3.1 画面構成と表示項目

| 項目 | 説明 | 状態 |
|------|------|------|
| カテゴリ・難易度バッジ | 単語の分類情報 | ✅ |
| イメージ画像 | カテゴリ別プレースホルダー/固有画像 | ✅ |
| 単語・発音・品詞 | ヘッダー表示 | ✅ |
| 発音記号 | /ˈskedʒuːl/ 形式 | ✅ |
| 音声再生ボタン | SpeakButtonコンポーネント | ✅ |
| 意味（日本語） | メイン表示 | ✅ |
| 記憶度 | 学習履歴から計算した習熟度 | ✅ |
| 例文 | 複数例文をハイライト表示 | ✅ |
| コアイメージ | 単語の核となるイメージ | 🔜 |
| 使い方 | 具体的な使用方法 | 🔜 |
| 類義語・対義語 | タップで詳細遷移可能 | ✅ |
| 類義語との違い | 使い分けの解説 | 🔜 |
| 関連語 | 関連する単語群 | 🔜 |
| 英英定義 | 英語による定義 | 🔜 |
| 語源 | 単語の由来 | 🔜 |
| コラム | 学習に役立つ豆知識 | ✅ |

### 3.2 記憶度の計算ロジック

```typescript
type MasteryLevel = "new" | "learning" | "familiar" | "mastered";

const getMasteryLevel = (accuracy: number | null, attempts: number): MasteryLevel => {
  if (attempts === 0 || accuracy === null) return "new";      // 未学習
  if (accuracy >= 80 && attempts >= 3) return "mastered";     // 習得済み
  if (accuracy >= 60) return "familiar";                       // 習得中
  return "learning";                                           // 学習中
};
```

### 3.3 単語詳細画面のUI構成

```
┌─────────────────────────────────────────────┐
│ ← 戻る                                       │
├─────────────────────────────────────────────┤
│  [ビジネス一般]  [初中級]                     │ バッジ
├─────────────────────────────────────────────┤
│                                             │
│  [     カテゴリイメージ画像     ]            │
│                                             │
├─────────────────────────────────────────────┤
│      schedule              🔊               │
│   /ˈskedʒuːl/ · 名詞                         │
│      予定、スケジュール                       │
├─────────────────────────────────────────────┤
│  📖 学習中     ━━━━━━━━━ 60%                 │ 記憶度
│    3回学習 / 正答率60%                        │
├─────────────────────────────────────────────┤
│  📝 例文                                     │
│  ・Please check my **schedule**.            │
│    私のスケジュールを確認してください。 🔊     │
├─────────────────────────────────────────────┤
│  💡 コアイメージ                             │
│    今後追加予定                               │
├─────────────────────────────────────────────┤
│  📝 使い方                                   │
│    今後追加予定                               │
├─────────────────────────────────────────────┤
│  🔗 類義語: [plan] [timetable] [agenda]     │
│  ↔️ 対義語: [chaos]                          │
├─────────────────────────────────────────────┤
│  🔍 類義語との違い                            │
│    今後追加予定                               │
├─────────────────────────────────────────────┤
│  🇬🇧 英英定義                                │
│    今後追加予定                               │
├─────────────────────────────────────────────┤
│  📜 語源                                     │
│    今後追加予定                               │
├─────────────────────────────────────────────┤
│  📚 コラム                                   │
│    「schedule」はギリシャ語の...              │
├─────────────────────────────────────────────┤
│           [この単語を復習する]               │
│           [ホームに戻る]                     │
└─────────────────────────────────────────────┘
```

---

## 4. ホーム画面仕様

### 4.1 表示要素

| 項目 | 説明 | 状態 |
|------|------|------|
| ユーザーレベル | 現在のレベルとXP進捗バー | ✅ |
| ストリーク表示 | 連続学習日数 | ✅ |
| デイリー目標進捗 | 今日の正解数/目標数 | ✅ |
| 学習メニュー | クイズ・スピードチャレンジへの導線 | ✅ |
| 統計への導線 | 学習履歴・実績への導線 | ✅ |

### 4.2 ホーム画面のUI構成

```
┌─────────────────────────────────────────────┐
│  🎓 英語学習アプリ                           │
├─────────────────────────────────────────────┤
│  Lv.5  ━━━━━━━━━━ 75%                        │ レベル進捗
│  🔥 3日連続学習中!                           │ ストリーク
├─────────────────────────────────────────────┤
│  📊 今日の目標                               │
│  正解数: 7/10  ━━━━━━━━━ 70%                 │
├─────────────────────────────────────────────┤
│  [📝 クイズに挑戦]                           │ メインアクション
│  [⚡ スピードチャレンジ]                      │
├─────────────────────────────────────────────┤
│  [📈 学習統計]  [🏆 実績]  [💪 苦手単語]      │ サブメニュー
└─────────────────────────────────────────────┘
```

---

## 4.5 単語帳画面仕様（/word-list）

### 概要

全500語を一覧表示し、検索・フィルター・ソート機能で効率的に単語を探せる画面。

### 機能一覧

| 機能 | 説明 | 状態 |
|------|------|------|
| 検索 | 単語・意味で絞り込み | ✅ |
| カテゴリフィルター | 8カテゴリで絞り込み | ✅ |
| 難易度フィルター | 難易度1〜5で絞り込み | ✅ |
| ソート | 複数の並び替えオプション | ✅ |
| 学習状況表示 | 習得度バッジ・難易度星 | ✅ |
| 統計サマリー | 全体/習得/学習中/未学習 | ✅ |

### ソートオプション

| オプション | 説明 |
|------------|------|
| デフォルト | ID順（データ定義順） |
| アルファベット順 (A→Z) | 英単語の昇順 |
| アルファベット順 (Z→A) | 英単語の降順 |
| 正答率が低い順 | 苦手な単語から表示 |
| 正答率が高い順 | 得意な単語から表示 |
| 学習回数が多い順 | よく学習した単語から表示 |
| 難易度順 | 難易度が低い順 |

### UI構成

```
┌─────────────────────────────────────────────┐
│ ← 単語帳                                     │
├─────────────────────────────────────────────┤
│  [500全単語] [120習得] [80学習中] [300未学習]  │ 統計サマリー
├─────────────────────────────────────────────┤
│  🔍 単語・意味で検索...                       │ 検索バー
├─────────────────────────────────────────────┤
│  [すべて][ビジネス][オフィス][旅行]... →       │ カテゴリ横スクロール
├─────────────────────────────────────────────┤
│  難易度: [全て][1][2][3][4][5]               │ 難易度フィルター
│  並び替え: [デフォルト ▼]                     │ ソートドロップダウン
├─────────────────────────────────────────────┤
│  250語 / 難易度3                              │ 結果カウント
├─────────────────────────────────────────────┤
│  🔊 schedule  [習得済] ★★★☆☆                │
│     スケジュール                              │
│  ─────────────────────────────────────────  │
│  🔊 meeting   [学習中] ★★☆☆☆                │
│     会議                                     │
│  ...                                         │
└─────────────────────────────────────────────┘
```

### 単語詳細への遷移

- 各単語をタップすると `/word/[id]?from=wordlist` に遷移
- 単語詳細画面から「単語帳に戻る」で復帰

---

## 4.6 ブックマーク機能

### 概要

お気に入りの単語を保存し、後から効率的に復習できる機能。

### 機能一覧

| 機能 | 説明 | 実装場所 |
|------|------|----------|
| ブックマーク追加/解除 | 単語をお気に入りに登録/解除 | 単語帳・単語詳細・ブックマーク一覧 |
| ブックマーク一覧 | ブックマーク済み単語を一覧表示 | /bookmarks |
| ブックマークフィルター | ブックマーク済み単語のみ表示 | 単語帳 |
| ブックマーク数表示 | 現在のブックマーク数を表示 | 単語帳 |

### ストレージ

- **キー**: `bookmarked_words`
- **形式**: `number[]` (単語IDの配列)
- **永続化**: localStorage

### storage.ts API

```typescript
// ブックマーク管理
getBookmarkedWordIds(): number[]       // ブックマーク済み単語ID一覧
isWordBookmarked(wordId: number): boolean  // ブックマーク状態確認
addBookmark(wordId: number): void      // ブックマーク追加
removeBookmark(wordId: number): void   // ブックマーク解除
toggleBookmark(wordId: number): boolean // 切り替え（新状態を返す）
```

### UI仕様

#### 単語帳画面

- ブックマークフィルターボタン（トグル式）
- 各単語行にブックマークアイコン（タップで切り替え）
- フィルターON時は黄色背景で強調

#### 単語詳細画面

- ヘッダー右上に「保存」ボタン
- 保存済みの場合は「保存済み」表示（黄色背景）

#### ブックマーク一覧画面 (`/bookmarks`)

ブックマーク済み単語を一覧で確認・管理できる専用画面。

**レイアウト**: Viewport-Fit Layout パターン

**画面構成**:

| エリア | 内容 |
|--------|------|
| 上部固定 | ヘッダー（タイトル「ブックマーク」、単語数） |
| 上部固定 | ソートボタン（登録順/名前順/難易度順） |
| 中央スクロール | ブックマーク単語リスト |
| 下部固定 | クイズ誘導カード |

**各単語の表示項目**:
- 難易度バッジ（Lv.1〜4）
- 単語名・意味
- カテゴリ
- 読み上げボタン（SpeakButton）
- ブックマーク解除ボタン（🔖アイコン）
- 詳細遷移矢印

**ソートオプション**:
- 登録順（デフォルト）: 最後にブックマークした順
- 名前順: アルファベット昇順
- 難易度順: Lv.1 → Lv.4

**空の場合**:
- メッセージ「ブックマークがありません」
- 単語帳への誘導ボタン

**遷移**:
- `/bookmarks` → `/word/[id]?from=bookmarks` → `/bookmarks`

---

## 4.7 カスタムクイズ機能

### 概要

クイズ開始前にカテゴリ・難易度・ブックマークを選択し、出題範囲をカスタマイズできる機能。

### クイズフェーズ

| フェーズ | 説明 |
|----------|------|
| setup | 出題設定を選択 |
| quiz | クイズ実行中 |
| result | 結果表示 |

### 設定項目

| 項目 | 説明 | デフォルト |
|------|------|-----------|
| カテゴリ | 8カテゴリから複数選択可 | 全カテゴリ |
| 難易度 | 1〜5から複数選択可 | 全難易度 |
| ブックマークのみ | ONでブックマーク済み単語のみ出題 | OFF |

### UI構成（設定画面）

```
┌─────────────────────────────────────────────┐
│ ← クイズ設定                                 │
├─────────────────────────────────────────────┤
│  カテゴリを選択                              │
│  [ビジネス] [オフィス] [旅行] [買い物]        │
│  [金融] [テクノロジー] [日常] [コミュニケ]    │
│  全カテゴリから出題                          │
├─────────────────────────────────────────────┤
│  難易度を選択                                │
│  [1] [2] [3] [4] [5]                        │
│  全難易度から出題                            │
├─────────────────────────────────────────────┤
│  [🔖 ブックマークのみ]          (25語)       │
├─────────────────────────────────────────────┤
│  対象: 500語                                 │
│                                             │
│  [10問で開始]                               │
│  設定をリセット                              │
└─────────────────────────────────────────────┘
```

### リザルト画面のアクション

- **同じ設定でもう1セット**: 前回と同じ設定で再挑戦
- **設定を変更して挑戦**: 設定画面に戻る
- **ホームに戻る**: トップページへ

---

## 5. 学習統計画面仕様（/history）

### 5.1 タブ構成

| タブ | 内容 | 単語詳細遷移 |
|------|------|-------------|
| 概要 | 全体統計、問題タイプ別正答率 | - |
| 苦手単語 | 正答率70%未満の単語リスト | ✅ |
| 履歴 | 回答履歴（最新50件） | ✅ |

### 5.2 概要タブ

- **総回答数・正解数・正答率**
- **問題タイプ別正答率**: 英→日 / 日→英 / 穴埋め
- **学習した単語数**: ユニークな単語のカウント

### 5.3 苦手単語タブ

- **抽出条件**: 正答率70%未満 かつ 1回以上挑戦
- **表示項目**: 単語、意味、正答率、挑戦回数
- **ソート**: 正答率の低い順
- **遷移**: タップで単語詳細画面へ

### 5.4 履歴タブ

- **表示件数**: 最新50件
- **表示項目**: 正解/不正解アイコン、単語、意味、問題タイプ、日時
- **遷移**: タップで単語詳細画面へ

### 5.5 学習統計画面のUI構成

```
┌─────────────────────────────────────────────┐
│  📊 学習統計                                 │
├─────────────────────────────────────────────┤
│  🔥 今日の学習                               │
│  回答数: 15   正解数: 12   正答率: 80%       │
├─────────────────────────────────────────────┤
│  [概要]  [苦手単語]  [履歴]                  │ タブ
├─────────────────────────────────────────────┤
│  💪 苦手単語 (5語)                           │
│  ┌─────────────────────────────────────┐   │
│  │ 🔊 confirm    確認する     45% →   │   │
│  │ 🔊 available  利用可能な   60% →   │   │
│  │ ...                                  │   │
│  └─────────────────────────────────────┘   │
│           [苦手単語を復習する]              │
└─────────────────────────────────────────────┘
```

---

## 6. 実績システム仕様

### 6.1 実績カテゴリ

| カテゴリ | 説明 | 例 |
|----------|------|-----|
| streak | 連続学習日数達成 | 3日連続、7日連続、30日連続 |
| score | 正解数達成 | 初めての正解、100問正解、1000問正解 |
| mastery | 単語習得数達成 | 10単語習得、50単語習得 |
| speed | スピードチャレンジ関連 | 初挑戦、10問正解、新記録達成 |
| special | 特別実績 | パーフェクト達成、コンボ達成 |

### 6.2 実績解除フロー

1. クイズ完了時に `storage.checkAndUnlockAchievements()` を呼び出し
2. 条件を満たす実績があれば自動解除
3. `AchievementUnlockPopup` でポップアップ表示
4. 実績一覧画面（/achievements）で確認可能

### 6.3 実績画面のUI構成

```
┌─────────────────────────────────────────────┐
│  🏆 実績                                     │
├─────────────────────────────────────────────┤
│  解除済み: 5/20                              │
├─────────────────────────────────────────────┤
│  ┌─────────────────────────────────────┐   │
│  │ 🥇 初めての正解        解除済み     │   │
│  │ 🔥 3日連続学習         解除済み     │   │
│  │ 🔒 7日連続学習         未解除       │   │
│  │ ...                                  │   │
│  └─────────────────────────────────────┘   │
└─────────────────────────────────────────────┘
```

---

## 7. 一人プレイMVP時点での制約と前提

### 7.1 データ永続化

- **ストレージ**: localStorage（ブラウザ）
- **ユーザー識別**: 固定値`"default"`（認証なし）
- **データ移行**: 将来的にSupabase DBへ移行予定

### 7.2 認証

- **MVP**: 認証なし（一人プレイ前提）
- **将来**: Supabase Auth導入予定

### 7.3 テスト

- **フレームワーク**: Vitest + Testing Library
- **配置**: `src/**/__tests__/*.test.ts(x)`
- **モック**: Web Speech API, localStorageは`vitest.setup.ts`で設定済み
- **実行**: `npm run test` / `npm run test:watch`

### 7.4 画像・音声

- **画像**: コンセプトベース画像選択（単語名→キーワード→カテゴリの順でフォールバック）
- **音声**: Web Speech API（ブラウザ内蔵）
- **画像拡張**: 単語データに`imageUrl`/`imageKeyword`フィールドを追加可能

### 7.5 単語データ

- **現在**: `src/data/words.ts`に静的定義
- **将来**: DB管理、外部API連携予定

---

## 8. 状態管理

### 8.1 セッション状態（メモリ）

```typescript
// クイズセッション
type QuizSessionState = {
  questions: Question[];
  currentIndex: number;
  selected: string | null;
  isCorrect: boolean | null;
  score: number;
  combo: number;
  maxCombo: number;
  isFinished: boolean;
};

// スピードチャレンジセッション
type SpeedChallengeState = {
  questions: Question[];
  currentIndex: number;
  score: number;
  combo: number;
  maxCombo: number;
  timeLeft: number;
  isFinished: boolean;
};
```

### 8.2 ユーザーデータ（localStorage）

```typescript
type UserData = {
  streak: number;              // 連続学習日数
  lastStudyDate: string | null;
  totalXp: number;
  level: number;
  dailyGoal: number;           // デフォルト: 10
  todayCorrect: number;
  todayDate: string | null;
};
```

#### XP・レベル計算ロジック

| 項目 | 計算式 |
|------|--------|
| 正解XP | 10 XP/問 |
| コンボボーナス | 5 XP/コンボ |
| レベルN完了に必要なXP | N × 100 XP |

**レベルアップ条件:**
- `totalXp >= level * 100` でレベルアップ
- 例: レベル2になるには100XP、レベル3になるには200XP必要

**XP進捗表示:**
- 現在のレベル内での進捗を表示
- 例: レベル2で150XP保有 → 「50 / 200 XP」（レベル3まで）

**データ整合性保護（`storage.getUserData()`）:**
- `totalXp` が負の場合は0に補正
- `level` が1未満の場合は1に補正
- `level` と `totalXp` の整合性を自動修復（不正データ対策）

### 8.3 学習記録（localStorage）

```typescript
type LearningRecord = {
  id: string;
  wordId: number;
  questionType: QuestionType;
  correct: boolean;
  studiedAt: string;
};
```

### 8.4 実績（localStorage）

```typescript
type UnlockedAchievement = {
  id: string;
  unlockedAt: string;
};
```

### 8.5 スピードチャレンジ結果（localStorage）

```typescript
type SpeedChallengeResult = {
  score: number;
  maxCombo: number;
  playedAt: string;
};
```

---

## 9. コンポーネント構成

### 9.1 UIコンポーネント（`src/components/ui/`）

| コンポーネント | 説明 |
|--------------|------|
| Button | 汎用ボタン（variant: primary/secondary/outline） |
| Card | カード型コンテナ |
| ProgressBar | 進捗バー（カスタマイズ可能な色） |
| Header | ヘッダー（戻るボタン付き） |
| SpeakButton | 音声再生ボタン（サイズ: sm/md/lg） |

### 9.2 機能別コンポーネント（`src/components/features/`）

#### 単語詳細（`word-detail/`）

| コンポーネント | 説明 |
|--------------|------|
| WordHeader | 単語・発音・品詞・意味 |
| WordImage | イメージ画像（フォールバック付き） |
| WordMastery | 記憶度表示（4段階） |
| WordExamples | 例文リスト（ハイライト付き） |
| WordRelations | 類義語・対義語 |
| WordColumn | コラム表示 |
| WordPlaceholderSection | プレースホルダーセクション |

#### 実績（`achievements/`）

| コンポーネント | 説明 |
|--------------|------|
| AchievementList | 実績一覧（カテゴリ別） |
| AchievementCard | 実績カード（解除状態表示） |
| AchievementUnlockPopup | 解除ポップアップ（アニメーション付き） |

---

## 10. ページ構成

| パス | 説明 | 状態 |
|------|------|------|
| `/` | ホーム画面 | ✅ |
| `/quiz` | クイズに挑戦 | ✅ |
| `/speed-challenge` | スピードチャレンジ | ✅ |
| `/word/[id]` | 単語詳細画面 | ✅ |
| `/weak-words` | 苦手単語一覧 | ✅ |
| `/history` | 学習統計（タブ: 概要/苦手単語/履歴） | ✅ |
| `/achievements` | 実績一覧 | ✅ |

---

## 10.5 レスポンシブデザイン

### 基本方針

- **PCでもスマホでも1画面に収まる**: リスト系画面は固定ヘッダー・固定フッターとスクロール可能な中央エリアで構成
- **詳細画面は自然なスクロール**: 情報量が多いページは`min-h`でスクロール可能に

### レイアウトパターン

#### Viewport-Fit Layout（リスト・ナビゲーション系画面）

スクロールなしで全要素にアクセスできる構造。以下の画面に適用:

| 画面 | パス | 適用 |
|------|------|------|
| クイズ設定 | /quiz (setup) | ✅ |
| クイズ問題 | /quiz (quiz) | ✅ |
| クイズリザルト | /quiz (result) | ✅ |
| スピードチャレンジ（準備） | /speed-challenge (ready) | ✅ |
| スピードチャレンジ（プレイ） | /speed-challenge (playing) | ✅ |
| スピードチャレンジ（リザルト） | /speed-challenge (result) | ✅ |
| 単語帳 | /word-list | ✅ |
| 学習履歴 | /history | ✅ |
| 苦手単語 | /weak-words | ✅ |
| ブックマーク | /bookmarks | ✅ |
| 実績 | /achievements | ✅ |

**CSS構造:**

```tsx
<div className="h-[calc(100vh-64px)] px-4 py-3 flex flex-col">
  <div className="max-w-{size} w-full mx-auto flex flex-col h-full">
    {/* 上部固定: ヘッダー・フィルター等 */}
    <div className="flex-shrink-0 mb-2">
      {/* 固定表示のコンテンツ */}
    </div>

    {/* 中央スクロール可能エリア */}
    <div className="flex-1 overflow-y-auto min-h-0">
      {/* リスト等のスクロールコンテンツ */}
    </div>

    {/* 下部固定: アクションボタン等（オプション） */}
    <div className="flex-shrink-0 pt-2">
      {/* ボタン等 */}
    </div>
  </div>
</div>
```

**重要なCSSプロパティ:**

| プロパティ | 用途 |
|-----------|------|
| `h-[calc(100vh-64px)]` | ヘッダー（64px）を除いた高さ |
| `flex flex-col` | 縦方向のフレックスレイアウト |
| `flex-shrink-0` | 固定サイズ（縮小しない） |
| `flex-1` | 残りスペースを埋める |
| `overflow-y-auto` | 縦方向スクロール有効 |
| `min-h-0` | フレックスアイテムの縮小を許可 |

#### Scrollable Content Layout（詳細・ダッシュボード系画面）

情報量が多く、自然なスクロールが適切な画面:

| 画面 | パス | 理由 |
|------|------|------|
| ホーム | / | ダッシュボード型で多数のカードを配置 |
| 単語詳細 | /word/[id] | 例文・関連語・コラム等の詳細情報 |

**CSS構造:**

```tsx
<div className="min-h-[calc(100vh-64px)] px-4 py-6">
  <div className="max-w-{size} mx-auto">
    {/* コンテンツ（自然にスクロール） */}
  </div>
</div>
```

#### ヘッダーナビゲーションのレスポンシブ対応

ヘッダーは画面サイズに応じて表示を切り替える:

| 画面サイズ | 表示内容 |
|-----------|---------|
| **PC (md以上)** | 主要ナビ5つ（ホーム、クイズ、スピード、単語帳、実績）を横並び表示、ラベルはlg以上で表示 |
| **スマホ (md未満)** | ハンバーガーメニューで全ナビゲーションを折りたたみ |

**ナビゲーションアイテムの分類:**

| 分類 | アイテム | 表示場所 |
|------|---------|---------|
| 主要ナビ | ホーム、クイズ、スピード、単語帳、実績 | PC: ヘッダー横並び / スマホ: ハンバーガーメニュー |
| サブナビ | 履歴、ブックマーク、お知らせ | UserMenuドロップダウン内（常時） |

**ユーザーメニュー（UserMenu）:**

| 状態 | ボタン表示 | ドロップダウン内容 |
|------|-----------|------------------|
| 未ログイン | ユーザーアイコン + 矢印 | ログイン・新規登録ボタン + サブナビ（履歴・ブックマーク・お知らせ） |
| ログイン済み | アバター + 矢印 | ユーザー情報 + プロフィール編集 + サブナビ + ログアウト |

**CSS構造:**

```tsx
// Header.tsx
const primaryNavItems = [
  { href: "/", label: "ホーム", icon: "🏠" },
  { href: "/quiz", label: "クイズ", icon: "📝" },
  { href: "/speed-challenge", label: "スピード", icon: "⚡" },
  { href: "/word-list", label: "単語帳", icon: "📖" },
  { href: "/achievements", label: "実績", icon: "🏆" },
];

const allNavItems = [
  ...primaryNavItems,
  { href: "/history", label: "履歴", icon: "📊" },
  { href: "/bookmarks", label: "ブックマーク", icon: "🔖" },
];

{/* PC用ナビゲーション（md以上で表示） */}
<div className="hidden md:flex items-center gap-2">
  <nav>{/* primaryNavItems */}</nav>
  <UserMenu />
</div>

{/* スマホ用（md未満で表示） */}
<div className="flex md:hidden items-center gap-2">
  <UserMenu />
  <button>{/* ハンバーガーメニュー: allNavItems */}</button>
</div>
```

### コンパクト化のポイント

| 項目 | 変更内容 |
|------|----------|
| パディング | `py-8` → `py-3` |
| マージン | `mb-4` → `mb-2` |
| フォントサイズ | `text-2xl` → `text-lg` |
| ボタン | `size="md"` → `size="sm"` |
| カード内パディング | `p-4` → `!p-3` |
| タブ・バッジ | `py-2` → `py-1.5` |

### 絵文字アイコンの表示パターン

絵文字とテキストを並べて表示する際、縦位置のズレを防ぐため以下のルールに従う。

**原因**: 絵文字は通常のテキストより行高(line-height)が大きく、flexbox の `items-center` だけでは視覚的な中央揃えができない。

**解決策**: `globals.css` に定義された `emoji-icon` クラスを使用する。

```css
/* globals.css */
.emoji-icon {
  @apply inline-flex items-center justify-center leading-none;
}
```

**使用パターン**:

```tsx
// 見出しでアイコンとテキストを並べる場合
<h2 className="flex items-center gap-2">
  <span className="emoji-icon">📈</span>
  <span>あなたの学習状況</span>
</h2>

// サイズ指定がある場合
<span className="text-2xl emoji-icon">🔥</span>

// アニメーションと組み合わせる場合
<span className="animate-float emoji-icon">⭐</span>

// コンテナ内で中央配置する場合
<div className="flex items-center justify-center">
  <span className="text-6xl emoji-icon">⚡</span>
</div>
```

**重要なルール**:
- 絵文字とテキストを同じ行に表示する際は、必ず両方を`<span>`で囲む
- 絵文字側には必ず `emoji-icon` クラスを付ける
- flex コンテナ内の絵文字は `emoji-icon` で行高を安定させる

---

## 11. 将来拡張に向けた設計方針

### 11.1 データ拡張

- 単語詳細項目（コアイメージ、英英定義等）はプレースホルダーとして枠を用意
- 型定義に拡張フィールドを事前定義
- データ追加時にUIの変更不要

### 11.2 認証対応

- `userId`を全データに含めて設計
- localStorage → DB移行を想定した構造
- `storage.ts`の内部実装を変更するだけで移行可能

### 11.3 音声・画像

- 外部URL対応の設計（`imageUrl`, `audioUrl`フィールド）
- Web Speech APIをフォールバックとして維持

### 11.4 追加予定機能

| 機能 | 優先度 | 説明 | 状態 |
|------|--------|------|------|
| 単語帳 | 高 | 全単語一覧表示・検索・フィルター・ソート | ✅ |
| ブックマーク | 中 | お気に入り単語登録・フィルター表示 | ✅ |
| 復習リマインダー | 中 | 忘却曲線に基づく通知 | 🔜 |
| カスタムクイズ | 中 | カテゴリ・難易度・ブックマーク選択 | ✅ |
| ダークモード | 低 | テーマ切り替え | 🔜 |
